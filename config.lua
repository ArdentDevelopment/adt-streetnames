cfg = {}

-- 1, 2, 3, 4
cfg.theme = 1

-- map icon in street name
cfg.mapicon = true

-- Street Name Position
-- 'top-left', 'top-center'
-- 'top-right', 'center-left'
-- 'center-right', 'bottom-left'
-- 'bottom-center', 'bottom-right'
cfg.position = 'bottom-right'

-- street name update intervals
cfg.rfstimes = {
    vehicle = 2500, 
    ped = 3000
}
